# file for storing ideas

# def check_canvas_size(rows, columns)
#     if ((rows >= 1) && (rows<=250) && (columns >= 1) && (columns<=250)
#       return 
#     else 
#       return ""
#   end
# end

# if integer_size_check(input_array.drop(1))
#     create_canvas(input)
#   else
#     puts "Please make sure the numbers you give me are between 1 and 250"
#   end